# v0.6 实施与验证记录

实施基线：master `d9c42f301bae79f213e6b88a6a3c8e5c1030a775`，分支 `codex/fix-v06-precise-editing`。计划与契约见 [spec](../specs/v06-editing.md)。

## 当前结果：原格式保真修复（2026-09-22）

用户明确确认“优先保真，保留上传原格式”。此前的转换验证记录保留在下方作为历史证据；当前流程已取消 DOC → DOCX 工作副本转换。

- 导入直接保存原始字节，DOC 输出 DOC，DOCX 输出 DOCX。当前本机 WPS 为 `12,1,0,28505`，预览 120 DPI。
- 授权本地稿件：工作副本和未修改下载与原稿字节相同；原稿与副本分别重新打开后，全部 9 页逐像素一致。真实文件及其派生数据没有进入 Git 或代码评审。
- 浏览器实际上传、修改第一页文字颜色、下载 DOC，再独立重新打开：全文文字无增删，只有目标页变化，第 2–9 页逐像素一致；原先缺显的公式完整保留。已查看实际公式页。
- 本地真实稿件另通过栏目字号、全文字号、局部四字修改和各自的重复应用检查。原生保存后检查 190 段、3 张表、25 条批注、22 个媒体部件和 6 个嵌入部件；批注关联及嵌入对象实际流保持。字号修改产生的自然重排仍单独留待人工复核。
- 合成原生 DOC 集成检查覆盖字符属性、段落属性、页边距、精确/倍数行距、布尔开关反向修改、连续两个原格式版本和撤销。DOC/DOCX 浏览器检查覆盖实际下载、原稿字节相等、字体默认值和跨上传确认隔离。
- 单元与故障注入覆盖原生 UTF-16/OLE/隐藏锚点映射、正文/对象/非目标格式篡改、批注重编号关联、明确保存元数据、字体声明、字符单位缩进和继承表格边距。原格式无法安全映射或保存发生实质性范围外变化时，失败且不登记新版本。

最终结果：90 项单元/故障注入通过；原格式原生集成、DOC/DOCX 浏览器流程通过；Python 编译、PowerShell 桥接语法和 `git diff --check` 通过。最终 `autoreview` 返回 clean，无接受的可操作问题。服务已重启并通过本机健康检查。代码、合成测试、产品说明与 spec 保留在当前开发分支；本轮没有提交或推送。

原生校验仅规范化明确等价的表示：字符单位缩进优先于随字号重算的磅值；完全等于表格已有显式边距的行级重复声明；批注 ID 通过批注位置映射而非丢弃关联；新增字体声明只能来自计划要求。其他布局或对象变化不能凭“WPS 自动生成”放行。表格边距继承依据 [Open XML 文档](https://learn.microsoft.com/en-us/dotnet/api/documentformat.openxml.wordprocessing.tablecellmargindefault?view=openxml-3.0.1)，字符单位缩进参见 [Apache POI 的 OOXML 说明](https://poi.apache.org/apidocs/dev/org/apache/poi/xwpf/usermodel/XWPFParagraph.html)。

无修改时不强制原生另存：只读快照记录当前原格式文件的实际属性；目标已满足时保留原始字节，并允许下载未修改工作副本。此行为由合成无修改测试及真实原格式重复应用共同验证，不以创建新版本作为成功条件。

以下为此前实现及修复记录，转换限制不代表当前原格式流程仍有相同问题。

## 实现清单

- [x] 版本绑定的 DocumentModel、字符区间、逻辑表格网格、栏目候选、角色证据与纠正。
- [x] 控件、确定性文字解析和显式采用的参考要求共用 EditIntent；未解析内容保留为阻断项。
- [x] 按具体目标与属性合并全局/明确局部例外，同优先级冲突阻断；冻结计划防篡改与版本检查。
- [x] 跨 run 字符修改、段落和单节页边距；未指定属性保持；字体脚本范围、三态开关、下划线。
- [x] 字符语义校验允许等价 run 拆分，其他主文档 XML 与非主文档部件保全检查。
- [x] DOC 容器准入、独立转换工作基线、前后预览、转换差异提示、用户明确采用；原文件保持。
- [x] 结构先验、Linux 渲染、字体环境/替代风险、人工版式复核和客户端兼容性分别记录。
- [x] 成功后原子登记不可变版本；无变更不登记；重复计划复用；继续修改、撤销、版本切换和保护迁移。
- [x] 实际浏览器上传、仅字号调整、二次搜索局部改色、版本下载、HTML 报告、撤销和冲突阻断。

## 测试与证据

运行环境：WSL Ubuntu，Python 3.12.3，LibreOffice 26.2.6.3，Playwright Chromium。依赖沿用项目 Linux 虚拟环境；新增 `olefile==0.47` 用于 DOC 只读容器检查。

本轮单元与故障注入结果：60 项通过（其中历史回归 31 项、新增 v0.6 29 项）。实际 Chromium 工作流的四组断言通过；合成下载结果再次以 OOXML 读取验证。旧版浏览器回归亦通过，覆盖确认失效及 v1→v2。

`tests/v06_doc_browser.py` 额外通过：合成 DOC 的转换基线未经确认时阻断执行、确认后可生成结果、原始 DOC 下载字节不变、更换稿件后新 v1 不沿用旧文件的版式确认。

收尾 `autoreview` 首次发现本文件被旧 `verification/` 忽略规则排除，已增加仅针对 `docs/verification/*.md` 的例外；运行产物仍被忽略。复审返回 clean，无接受的可操作问题。

`tests/test_v06.py` 对应 V06-A01–A14，并覆盖部分重叠选区、合并网格、字段保护、超链接、多文本节点、空批注锚点、字号复杂文字表示、计划篡改、正文/对象/包部件故障注入、缺字体、渲染失败与幂等。原 v0.5 的 31 项单元回归保留。

`tests/v06_browser.py` 在真实 Chromium 中操作应用和下载文件，独立读取下载 DOCX 核对范围及非目标格式。证据写入被 Git 忽略的 `verification/v06-browser/`；不会把运行截图当成全量客户端兼容性证明。

`tests/local_sample_acceptance.py` 只接收显式的本地文件路径，强制输出目录位于仓库外。已用一份用户授权的真实 DOC 执行转换、栏目填写区字号、全文字号、四字局部修改和幂等检查，原稿哈希保持。结构、属性、对象及非主文档包部件检查通过，Linux 渲染成功。原稿和工作副本均渲染为 13 页；提取的可见文本不完全一致，因此转换状态保持待复核，不宣称无损转换。

真实样本的原件、转换件、候选输出、正文、批注、页面和详细报告均留在仓库外。仓库只保留通用测试代码、合成样本生成代码和本节非敏感汇总，不公开真实文档内容。

本地对照联系表覆盖原始 13 页、工作基线 13 页、修改候选 14 页，确认可见转换重排和字号变更引起的分页变化。该检查是整体对照，不是逐页原尺寸的合格成稿验收；人工版式状态继续保留待复核。

## 验收状态

| 场景 | 自动化证据 | 边界 |
| --- | --- | --- |
| A01–A02 | 只改字体/字号、混合格式保留；A02 另有浏览器下载核对 | 目标字体必须实际安装 |
| A03–A04 | 第二处重复文字、跨 run 四字修改、图片公式批注周边保全 | 公式/字段内部不开放编辑 |
| A05–A06 | 全局/局部按属性覆盖、输入顺序不影响结果、段落扩大范围提示 | 仅明确例外提升优先级 |
| A07–A08 | 栏目填写区与表头分离、横纵合并、正文是否包含表格切换 | 栏目候选需核对；弱角色不默认为正文 |
| A09–A10 | 二次修改保留旧属性，后续明确全局覆盖旧例外，保护仍生效 | 会话内版本，无跨会话恢复 |
| A11 | 冲突、未知范围、部分理解、缺单位阻断；取消记录保留 | 自然语言为有限语法，不宣称任意句式理解 |
| A12 | 合成复杂对象与一份本地真实 DOC 的结构/保全/渲染检查 | DOC 转换有文本差异待复核；Word/WPS 实机打开未运行 |
| A13 | 版本撤销、旧计划拒绝、失败原子性、浏览器撤销 | 原始 DOC 与工作基线分别保留 |
| A14 | 按钮与文字适配器生成相同格式和实际操作 | 仅已支持且明确的语法 |

## DOC 组合公式缺显修复（2026-09-21）

修复基线为 `7f7d316`。一份本地 DOC 的浮动图片组包含 6 个矢量公式片段；LibreOffice 直接渲染 DOC 可见，保存为 DOCX 后重新打开整组缺显。改变导出过滤器或使用 VML 回退未解决；保留原媒体并按组坐标展开为独立浮动图片后恢复。此结论来自本地隔离实验，不把“部件存在”当成“页面显示完整”。

修复位于 `v06_import.py`，仅在 DOC 转换后、建立工作基线前运行。原稿不变，所有图片/关系及其他非修改部件保持原字节；纯图片组的平移和缩放纳入坐标计算，旋转、翻转、环绕、相对尺寸或不支持的组结构直接阻断。上传的原生 DOCX 不走该兼容改写。导入版本标识变更，使已有会话重新导入旧基线。

转换文字检查新增字符计数与顺序两项证据；原有字符未检出时，页面确认与服务执行同时阻断，手动设置确认状态不能绕过。UI 使用明确的差异提示替代裸布尔值。这仍不是完整视觉等价检查，图片中不可提取的文字、分页和字体变化继续需要独立核对。

验证：完整 67 项单元/故障注入通过；新增 7 项覆盖媒体保留、坐标换算、拒绝未知变换、DOCX 原样导入、修复早于预览、缺字阻断和读取顺序差异。`tests/v06_import_render.py` 用独立生成的 WMF 验证 6 个公式在导入和后续字号修改后均可渲染并提取。原 v0.6 浏览器四组断言及 DOC 转换/原件下载/跨上传隔离回归通过。`autoreview` 返回 clean，无可操作问题。

真实样本重新导入后 6 个公式片段恢复可见，未检出字符从 22 降为 0，转换前后均为 13 页；仍额外提取 1 个列表标点并存在换行重排，完整转换版式状态继续待复核。原稿哈希保持不变。真实样本、修复副本、图片和详细诊断全部位于仓库外，未作为代码评审资料或 Git 内容发送。

真实样本另通过 Chromium 上传、切换到问题页预览、指定一段文字调整字号、确认并下载 DOCX 的完整路径。独立重渲染下载文件确认公式仍显示，非主文档部件与对应转换基线字节一致，下载的原始 DOC 与上传字节一致。浏览器脚本等待 Streamlit 完成当前重跑再发下一次操作，避免测试事件相互取消；未把本地真实内容写入合成回归脚本。

## 剩余边界

### 全文导入检查与本机引擎（2026-09-22）

上次修复只恢复组合公式，未消除跨引擎转换造成的字体、编号和分页变化。本轮隔离试验确认，向 Linux 渲染器补齐字体仍不能保证 DOC/DOCX 分页一致。现接入本机 WPS 保留兼容模式的转换；应用仍在 WSL 运行，转换和前后预览均在本机处理，不使用在线服务。

一份授权本地样本通过原 DOC 和转换后独立重新打开 DOCX 的全篇对照：全文文字顺序一致，缺字/多字均为 0，前后均为 9 页，8 页在 120 DPI 逐像素一致；剩余一页的一行字存在约 0.12 磅位置差异，已记录差异区域，不宣称整篇画面完全一致。此前缺显的公式页逐像素一致。重复原稿渲染稳定；重新分页、另一保存格式及 run 合并/拆分试验未消除该细微差异，未将这些试验性改写引入产品。

新增 `tests/test_v06_fidelity.py` 的 13 项故障注入通过，覆盖新增字符、分页变化、无文本图片差异、最后一页与尺寸、预览缺页、文字调序、显式引擎选择、失败不切换、WPS 不走 LibreOffice 补丁、占用错误和临时文件清理。既有 67 项单元检查通过；合成 DOCX 浏览器四组断言及合成 DOC 导入/原稿下载/跨上传隔离通过。LibreOffice 的六组合成矢量公式渲染回归单独指定引擎后通过。本机同时打开一份合成稿件的隔离检查确认，桥接未改变已有稿件数量、可见性、宏安全和链接更新设置。

真实样本另通过浏览器上传、检查文字/页数/实际差异提示、调整一段字号、确认下载、独立重渲染路径。下载原稿字节不变，修改后全文文字仍一致，公式页与原稿逐像素一致；被修改段落引发的排版变化与导入残余字距差异分别保留。全部真实产物在仓库外，未提供给代码评审或 Git。

最终完整单元检查为 80 项通过。`tests/v06_native_browser.py` 在重启后的服务中确认字体默认值来自当前引擎已安装字体，实际应用格式并下载后文字保持；Python 编译及 `git diff --check` 通过。本机引擎记录 WPS 实际二进制版本 `12,1,0,28505`，而非 COM 兼容接口版本。最终 `autoreview` 返回 clean，无接受的可操作问题；评审仅包含代码、合成测试和通用汇总。

真实教案可能已有公式重叠或分页问题；修改字号也会引起自然重排。系统提供页面与独立人工状态，不自动宣布全部版式正确。客户端兼容性仍需指定版本的 Word/WPS 实测。单个真实样本不支持推广成所有复杂文档兼容结论。

修订、内容控件、文本框、多节、宏、签名、外部模板和外链对象继续阻断；表格布局、编号重排、页眉页脚编辑、增强模板/批注规则提取、个人规范库、跨会话存储属于后续能力。

LibreOffice 临时配置的链接更新值依据 [Writer 配置定义](https://github.com/LibreOffice/core/blob/master/officecfg/registry/schema/org/openoffice/Office/Writer.xcs)：`Content/Update/Link=2` 表示 Never。宏安全级别为 3，同时关闭自动字段/图表更新；这些仅用于本地转换与预览，不改用户桌面配置。
