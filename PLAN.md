# 文序 PRD v0.5 POC 实施计划

## 目标与完成口径

POC 要证明一条可信的单文档闭环：`稿件 + 文字要求/参考实例 → 完整候选规则 → 人工核对范围与规则 → 固定快照 → 属性级修改 → 自动检查与 Linux 渲染 → DOCX/报告另存 → 从结果继续局部调整`。

完成口径以 PRD F01–F06 和 A01–A12 为主。A13 的 Word/WPS 客户端结论、真实业务 30 份样本与试点指标需要外部样本和客户端环境，不能由合成数据替代。F07–F10 按 PRD 属于第一期/第二期。

## 已实现架构

| 层 | 实现 | 责任 |
|---|---|---|
| 单页流程 | `app.py` / Streamlit | 输入、核对、确认失效、执行、预览、下载、会话内版本与交付观察 |
| 包级保全 | `docx_formatting.py` | DOCX ZIP/XML 安全检查、复杂结构阻断、旧版格式能力与回归校验 |
| PRD 格式引擎 | `prd_formatting.py` | 结构映射、有效格式计算、多对象多属性快照、最小写回、严格保全与规则校验 |
| 要求与交付 | `prd_workflow.py` | 文字/实例提取、完整性与冲突、字体检查、Linux 渲染、HTML 报告、幂等指纹 |
| 验收 | `tests/` | 单元/故障注入、真实 Chromium 主流程、30 份合成矩阵 |

## 实施阶段

### 1. 输入与结构基线

- 对实际 DOCX 包、大小、解压规模、主文档类型、保护、外部关系和未支持正文结构做预检。
- 限定单节 A4 纵向，并列出段落、表格、图片、页眉页脚和外部网页链接数量。
- 从样式、大纲层级和文本位置建立稳定段落清单；弱证据交给用户确认。

门槛：输入失败有具体原因，不清空已填要求；新输入使旧确认和旧结果失效。

### 2. 双入口与确认表

- 文字要求按对象和属性拆行，保留来源原句。
- 参考实例只读取可识别的有效格式和位置，不复制正文。
- 缺对象、值或单位以及无法支持的表达进入待补充；同对象同属性不同值进入冲突。
- 人工编辑后的表格是唯一执行依据；应用、排除和保留范围进入快照与报告。

门槛：有已选阻断项时不能确认；确认后任一输入、范围或规则变化立即失效。

### 3. 属性级执行与保全

- 支持字符、段落、标题、页面和落款属性，使用有效格式判断直接值与继承值。
- 只写确认范围内尚未满足的属性，不修改共享样式或重建正文。
- 其他 ZIP 部件逐字节保全；正文 XML 屏蔽允许变化后必须完全一致。

门槛：目标规则逐项达到；内容、对象、未选属性和原稿保持；相同快照重复执行字节一致。

### 4. 渲染、报告与版本

- 输入和候选输出均由 WSL 内 Linux LibreOffice 转 PDF，再用 PyMuPDF 生成逐页预览。
- 缺少要求字体、超过 50 页、转换/光栅化失败或严格校验失败均阻断下载。
- 成功时原子分配 `_vN`，生成独立 HTML 报告和诊断 JSON；从 v1 局部调整生成 v2，保留 v1 其他属性。

门槛：真实浏览器完成上传到下载；输出可重新打开；报告可追溯输入、规则、检查与环境。

### 5. 验收与扩展决策

- 单元/故障注入覆盖 A01–A12 的引擎行为。
- 浏览器覆盖文字入口、确认失效、v1、报告、v1→v2。
- 合成矩阵覆盖 30 份、3 类、10 对要求变更、20/10 分组、三次幂等和逐份 Linux 渲染。
- 获得真实脱敏样本后冻结真实开发集/留出集，并在目标 Word/WPS 版本执行 A13。

## 风险边界

- `python-docx` 不代表任意 OOXML 都可安全往返；未进入验证子集的结构继续阻断。
- LibreOffice 预览能发现渲染失败和明显异常，但不等于 Word/WPS 显示一致。
- 字体名称写入 DOCX 不等于嵌入字体；固定环境必须安装要求字体。
- 当前版本与任务只存在会话内存；服务端恢复、权限、规范发布和批量隔离留给后续阶段。
