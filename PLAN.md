# DOCX 常见格式调整 POC 开发计划

## 2026-09-18 已实现扩展

在原核心流程上加入已有标题识别和字体选择。识别基于 DOCX 标题样式、大纲级别及样式继承，支持一至九级、正文与文档标题/副标题；不猜测只加粗或带序号的普通文本。用户可查看识别与作用范围后，仅对该范围应用宋体/黑体、字号、颜色、行距。范围外段落和未选属性由写回校验保护，字体同时设置中文与西文字体属性。无新增运行依赖。

此项扩展替代下文初版中“标题层级暂不做”的限制；自动推断未标记标题、新建标题层级、编号与论文规范仍是后续工作。验证方法和结果见 VERIFICATION.md。

## 2026-09-17 执行口径

用户已批准开始开发，并将本轮验收明确为：**Python 在 WSL 运行，前端上传文件、修改固定参数、下载的 DOCX 实际发生对应修改**。下面原计划中的 LibreOffice/PDF 预览、缺字体检测和 Word/WPS 客户端兼容矩阵移至后续阶段，不作为本轮核心流程的完成条件。实际运行和测试方法见 README.md，证据见 VERIFICATION.md。

已实现 Streamlit + python-docx + lxml；固定参数为字号、RGB 颜色、固定/倍数行距。写回仅替换主文档 XML，其余 ZIP 部件保留原字节；输入预检、未选属性保全校验、真实浏览器上传下载检查保留。此轮未安装或调用 LibreOffice。

**目标：** 做一个无 AI 调用的轻量 POC，让用户上传普通 DOCX，通过固定控件调整指定文本范围的字体颜色、字号和行距，并下载新的 DOCX。

**技术方案：** 使用 Streamlit 实现单页上传、格式选项、执行和下载；使用 python-docx 读取文档并只写回用户选择的格式属性；使用 LibreOffice headless 生成 PDF 预览并参与结果检查。输入原文件不覆盖。POC 仅声明经过样本验证的 DOCX 子集，不把库的格式支持等同于产品兼容性。

**技术栈：** Python 3.11+、Streamlit、python-docx、LibreOffice headless；pytest 仅在测试复杂度确有需要时加入，第一版优先用标准库 unittest。

## 1. 选型结论

| 层 | 选择 | 理由与边界 |
|---|---|---|
| UI | Streamlit | POC 需要上传、颜色/数值控件、执行按钮和文件下载；其内置控件覆盖这些交互，不额外搭 React、API 服务和状态管理。Apache-2.0。 |
| DOCX 读取/修改 | python-docx | Python 生态中直接处理 DOCX 的轻量选择，MIT；只改字体颜色、字号和段落行距，不抽取纯文本重建文档。 |
| 预览渲染 | LibreOffice headless | 生成 PDF 供用户预览和基础检查；渲染结果不作为修改输入，不能代替 Word/WPS 兼容性验收。 |
| AI/Agent | POC 不引入 | 所有 POC 选项是显式控件，无需自然语言理解；避免推理费用、结果不稳定和额外确认链路。 |

备选方案中，FastAPI + 前端会引入本 POC 不需要的 API/前端分层；docx4j 是功能更完整的 Java OOXML 路径，但当前没有 Java 项目或复杂文档需求支撑这项成本。若 python-docx 对目标样本的往返保全测试不通过，应重新评估基于 OOXML 包局部修改或 docx4j，而不是扩大承诺范围。

参考： [Streamlit 上传控件](https://docs.streamlit.io/develop/api-reference/widgets/st.file_uploader)、[Streamlit 下载控件](https://docs.streamlit.io/develop/api-reference/widgets/st.download_button)、[Streamlit 许可说明](https://streamlit.io/terms-of-use)、[python-docx 仓库与许可](https://github.com/python-openxml/python-docx)、[python-docx 段落格式语义](https://python-docx.readthedocs.io/en/latest/user/text.html#paragraph-format)。

## 2. POC 范围

### 必须做

- 单文件 `.docx` 上传；校验扩展名之外的实际包结构，限制文件大小，并对损坏或不支持输入给出明确原因。
- 三类独立可选修改：字体颜色、字号（磅）、行距（倍数或固定磅值）。未选择的属性不写回。
- 首版范围固定为文档正文中的普通段落；支持范围内的表格单元格段落也纳入处理。页眉页脚、文本框、脚注/尾注、修订/批注、嵌入对象等不保证可修改；检测到无法验证的关键结构时阻止导出并说明原因。
- 通过已有 run 和 paragraph 对象做属性级修改，保留文字、run 切分、加粗/斜体/下划线、图片、表格及其他未指定属性；不覆盖源文件。
- 输出使用新文件名（如 `原文件名_v1.docx`），展示本次修改摘要；能渲染时提供 PDF 预览。
- 修改前后做最小校验：DOCX 可重新打开、文本内容与顺序一致、文档结构未出现已知损坏，已选属性达到目标值。

### 明确不做

- Agent、LLM、自然语言规则抽取、参考样例自动分析、模板库和用户自定义风格。
- 任意段落语义识别或段落范围编辑；首版明确告知应用范围，避免暗示能理解“正文/标题”等语义对象。
- 标题层级、项目符号/编号（实心/空心/罗马数字）、目录、论文规范、批量处理、账号、历史、云端留存、团队审批。
- 对任意复杂 DOCX 或所有 Word/WPS 版本作无损保证。

## 3. 文件结构

计划编写时从空项目开始；当时工作区只有 PRD DOCX，没有可复用的原型代码。当前工作路径已关联 `https://github.com/GZUCMHjy/wenxu-docx.git`，使用 `master` 分支，并承接远程已有的 PRD、计划与 README 提交历史。

```text
app.py                 Streamlit 单页界面和流程编排
docx_formatting.py     输入检查、属性修改、结果校验
tests/test_docx_formatting.py  最小回归检查
requirements.txt       运行依赖及受测版本范围
README.md              本地启动、支持边界和已知限制
```

不拆独立 API、不建插件式引擎、不引入通用模板抽象。等出现第二种产品入口或第二种格式后再抽象。

## 4. 实施阶段与验收门槛

### 阶段 A：输入与保全基线

建立代表性样本和 DOCX 结构预检；在任何写回前记录文本/结构基线。门槛：损坏文件、伪装文件、超限文件可阻止；普通支持样本可以打开、保存、再次打开；文本和顺序一致。

### 阶段 B：确定性格式写回

先实现未勾选属性不变，再实现字号、颜色、行距分别更新；记录修改计数和目标值。门槛：每个单项/组合用例通过，未选择属性以及字符强调、图片、表格结构不因格式写回而改变。

### 阶段 C：用户界面与文件交付

用 Streamlit 串联上传、控件、预检、执行、摘要和下载。门槛：无效输入不能执行；成功时原稿仍可用，下载文件名新建且可重新打开。

### 阶段 D：渲染与试点判断（后续阶段）

用 LibreOffice 渲染修改前后样本为 PDF；记录字体缺失或分页变化。人工抽样用 Word/WPS 打开输出。门槛：不以 PDF 能生成代替内容保全或客户端兼容验收；任何内容差异、文件不可打开或必需字体缺失均阻断试点。

## 5. 主要风险与处置

- **python-docx 往返会影响未建模 OOXML 部件：** 以 PRD 支持边界筛样本；逐类增加样本并比较包部件、内容和渲染。关键结构未通过即拒绝该类输入；不静默降级。
- **全局范围可能改到标题或表格文字：** UI 明示“应用到文档正文全部段落（含表格段落）”；用户未选择属性则保留其现值。若用户需要只改正文/标题，作为后续独立范围识别能力，而不是暗中猜测。
- **字体和分页依赖环境：** 预览记录渲染环境；缺字体提示或阻断，不将替代字体当作等价结果。
- **有敏感文件：** POC 本地运行、不上传外部模型/云存储；处理结束清理临时文件，且不得记录正文或上传内容。

## 6. POC 成功标准

1. 用户不输入自然语言，也能通过固定控件完成至少一种指定格式修改。
2. 只修改被选择的属性；未选属性和正文内容保持不变。
3. 原稿不覆盖，输出 DOCX 可重新打开，并有修改摘要。
4. 支持边界、失败原因、渲染环境清晰可见；至少用一组普通段落、混合 run、表格和图片样本进行验证。
5. 在受控试点样本上验证可靠性后，才决定是否增加标题/编号或 Agent 自定义风格。
