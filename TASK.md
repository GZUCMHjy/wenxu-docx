# 文序 PRD v0.5 POC 任务清单

## F01 导入与预检

- [x] 校验 DOCX 实际包结构、10 MB 文件上限、60 MB 解压上限和 2000 部件上限。
- [x] 阻断损坏、加密、宏、签名、编辑保护、危险 XML、外部模板/对象和未支持正文结构。
- [x] 限定单节 A4 纵向；展示段落、表格、图片、页眉、页脚和外部网页链接清单。
- [x] 新稿件按内容指纹识别，替换输入时撤销旧确认和结果，保留已填要求。

## F02 双入口与人工确认

- [x] 文字要求拆成对象、属性、目标值、单位和来源行，无法解析项不静默丢弃。
- [x] 参考实例提取标题、正文和落款的有效格式及段落位置，不复制实例正文。
- [x] 缺对象/单位和值标为待补充；同对象同属性不同目标标为冲突；相同要求合并来源。
- [x] 要求表支持补充、编辑和取消应用；已选阻断项解决前不能确认。
- [x] 人工确认形成输入、规则和映射指纹；任一相关变化撤销确认。

## F03 结构识别与例外

- [x] 从直接大纲、样式继承、标题样式和文本位置生成标题一至三、正文、落款候选。
- [x] 显示原文、识别结果、依据和弱证据，不显示未经校准的置信度百分比。
- [x] 用户可逐段改为标题、正文、落款或原样保留；原样保留理由进入报告。
- [x] 新输入重新建立映射，不沿用旧段落序号。

## F04 基础格式应用

- [x] 字符：字体、字号、加粗、斜体和 RGB 颜色。
- [x] 段落：对齐、首行/左右缩进、段前/段后、固定或倍数行距。
- [x] 标题：三级角色和与下段同页；不新增、删除或重排编号文字。
- [x] 页面：单节 A4 纵向和四边页边距。
- [x] 落款：字体、字号、对齐和段落间距，文字不变。
- [x] 计算直接格式和样式继承后的有效值；已满足属性不写回。

## F05 检查与导出门槛

- [x] 严格比较正文、对象、未选属性和非主文档包部件。
- [x] 逐规则校验最终有效值；错误属性、缺字/缺图和关系变化会被阻断。
- [x] 检查要求字体是否存在；缺字体时不提供最终 DOCX。
- [x] WSL Linux LibreOffice 转换输入/输出，PyMuPDF 逐页预览，失败或超过 50 页时阻断。
- [x] 展示通过、警告和客户端边界；规则/范围变化不复用旧检查。

## F06 下载、版本与反馈

- [x] 输出 `_vN.docx`，输入名已有 `_v12` 时从 v13 起；编号只在成功输出时分配。
- [x] 生成独立 HTML 报告与诊断 JSON，不向正文写入水印或摘要。
- [x] 相同输入和快照复用任务结果；全部已符合、失败或取消不创建成功版本。
- [x] 会话内可选原稿或任一结果继续；真实浏览器已验证 v1 局部改行距得到 v2。
- [x] 已提交、被退回和已接收由用户记录，不把下载按钮点击视为业务接收。

## WSL 验收

- [x] 31 项单元与故障注入测试通过。
- [x] Linux LibreOffice 26.2.6.3 与 Noto CJK 用户字体实际渲染通过，中文无方框。
- [x] WSL Chromium 145 + Playwright 1.58 完成真实上传、确认、v1/v2 下载和报告检查。
- [x] 30 份合成矩阵通过：3 类文种、10 组要求变化、20/10 开发与留出分组、33 个规则实例、三次幂等、逐份渲染。
- [x] 示例稿件改为单节 A4 纵向并完成页面人工检查。

## 需要外部试点证据

- [ ] 用 30 份经授权的真实脱敏材料替换合成矩阵，并在验收前冻结 10 份留出集。
- [ ] 在试点指定的 Microsoft Word 与 WPS 版本执行 A13，记录版本、字体包、修复提示和人工对照结果。
- [ ] 记录真实用户完成时间、独立完成率、格式返工、反馈覆盖率和失败分类。

## 后续阶段

- [ ] 第一期 F07：个人规范库和不可变规则版本。
- [ ] 第一期 F08：服务端历史、关闭页面后的任务恢复和文件可用期。
- [ ] 第二期 F09：组织权限、送审、发布和生效时间。
- [ ] 第二期 F10：批量隔离、重试、部分交付和 ZIP 清单。
