# DOCX 格式调整 POC 任务清单

按依赖顺序执行。每项必须满足验收条件后再进入下一项；任一内容保全门槛失败，先缩小支持范围或更换写回方案，不得绕过检查继续宣称成功。

## 0. 开工准备

- [ ] 确认运行环境包含 Python 3.11+；为项目建立独立虚拟环境。
- [ ] 安装并记录 Streamlit、python-docx 的确切受测版本；安装 LibreOffice headless 并记录版本和可执行路径。
- [ ] 从 PRD 的 POC 边界挑选脱敏 `.docx` 样本：普通段落、混合格式 run、表格、图片；另准备损坏文件和伪装扩展名文件。
- [ ] 固定受支持输入声明：普通 `.docx`；不支持 `.doc`、加密/损坏文件及无法验证的复杂结构。

验收：新环境可按 README 重建；样本不含试点用户敏感信息。

## 1. 建立最小项目

- [ ] 新建 `requirements.txt`，仅列 `streamlit`、`python-docx` 及被代码直接使用的依赖，并锁定已验证版本。
- [ ] 新建 `docx_formatting.py`、`app.py`、`tests/test_docx_formatting.py`、`README.md`。
- [ ] 写 README 的启动命令、支持边界、临时文件处理和当前限制。

验收：`streamlit run app.py` 能启动空白 POC 页面；`python -m unittest discover -s tests` 可运行。

## 2. 输入预检与保全基线

- [ ] 先写失败测试：拒绝非 DOCX ZIP、损坏 ZIP、空文件和超过设定上限的文件。
- [ ] 实现基于 ZIP 包结构的实际 DOCX 校验；不能仅相信文件名后缀或浏览器 MIME 类型。
- [ ] 检查关键 OOXML 部件和不支持特征；对无法安全处理的输入返回可展示的原因，不尝试“修复后继续”。
- [ ] 建立文档基线提取：主文档段落文本顺序、表格文本、图片/关系数量和关键部件列表。
- [ ] 为所有支持样本执行原样 load/save/reopen 往返检查，发现内容或关键关系变化即停止该类样本支持。

验收：有效支持样本可往返；损坏/伪装/超限输入明确失败；原始字节不被覆盖。

## 3. 实现属性级修改

- [ ] 为字体大小、字体颜色、行距分别写最小失败测试，验证只修改目标属性。
- [ ] 实现 `apply_formatting(docx_bytes, options) -> (output_bytes, change_summary)`；`options` 仅包含被用户启用的字段。
- [ ] 字体颜色和字号更新正文段落及表格单元格段落中已有 runs 的直接格式，不合并/重建文本 runs。
- [ ] 行距仅更新对应段落的行距属性，不写入字体、字号、缩进和段前段后。
- [ ] 无 runs 的空段落不造文本；未选择的属性不触碰；遇到不支持结构抛出可识别错误而非部分成功。
- [ ] 保存到内存缓冲区，不写回上传源文件；生成清晰修改摘要和新版本文件名。

验收：字号、颜色、行距单项和组合用例通过；正文字符与顺序一致；粗体/斜体等其他 run 格式保持；原稿文件哈希不变。

## 4. 结果校验

- [ ] 输出后再次读取 DOCX，验证其可打开、文本与顺序等于输入基线。
- [ ] 检查用户选中的属性确实写入目标段落/runs；未选属性保持原值。
- [ ] 比较关键 OOXML 部件、图片和关系；任何丢失、损坏或内容差异都阻断下载。
- [ ] 将处理错误分为输入不支持、格式值无效、写回失败、结果校验失败，供 UI 显示。

验收：验证失败时不会返回“成功”状态或 DOCX 下载按钮。

## 5. Streamlit 交互

- [ ] 提供单文件上传和上传大小限制。
- [ ] 提供三个独立启用项：字体颜色（颜色选择器）、字号（磅数输入/受限选项）、行距（单倍/1.5 倍/固定磅值）。默认均不启用，不预填任何“规范值”。
- [ ] 明示作用范围为文档正文全部段落（含表格单元格段落）；页眉页脚等不在支持范围内。
- [ ] 没有启用任何修改项时禁用执行；预检失败时保留页面状态并阻断执行。
- [ ] 执行后展示修改摘要、检查结果和独立下载按钮；文件名为原名加 `_v1`，不覆盖原稿。
- [ ] UI 不显示“合规/符合单位规范”等未经规则库验证的结论。

验收：浏览器手动走通成功、无选项、损坏文件、超限文件四条路径。

## 6. 渲染预览与兼容性检查

- [ ] 对校验通过的输出调用 LibreOffice headless，将 DOCX 转成 PDF；超时、渲染错误或 PDF 空文件均显示失败，不复用旧预览。
- [ ] 给原稿和输出生成对照预览；记录渲染器版本与缺字体信息。
- [ ] 对脱敏代表样本用 WPS 或 Microsoft Word 打开输出，核对字体/字号/行距、中文显示、表格、图片、页眉页脚及内容。
- [ ] 将已验证样本和环境条件记入 README 的“验证记录”；未验证的结构明确标记不支持。

验收：DOCX 可打开、内容校验通过且预览成功才显示完整成功结果；缺字体或结果内容不一致阻断交付。

## 7. POC 交付门槛

- [ ] 从干净环境按 README 重建并完整执行测试。
- [ ] 用每一份支持样本重复处理至少 3 次，输出校验结论一致。
- [ ] 确认源文件未被覆盖，临时文件在会话结束/异常后清理。
- [ ] 输出已支持/不支持格式列表、已知风险和试点反馈问题；不实现账号、历史、批处理、模板库或 Agent。

验收：以上全部勾选，且失败案例均被阻断，才进入受控 POC 试用。
