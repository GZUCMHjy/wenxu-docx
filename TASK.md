# DOCX 格式调整 POC 任务清单

## 2026-09-18 标题与字体扩展

- [x] 按已有标题样式/大纲级别识别一至九级标题，支持自定义样式继承及中文样式名称。
- [x] 文档标题/副标题单列；正文作为非标题段落，不按粗体或编号文字猜测层级。
- [x] 前端增加范围选择、识别清单、识别依据和选中段落查看；没有匹配段落时阻止执行。
- [x] 字体独立启用，固定提供宋体和黑体；设置中文/西文字体属性，去除对应主题引用。
- [x] 范围外段落与未选属性严格校验；范围或参数变更撤下旧下载结果。
- [x] WSL 共 17 项单元测试通过；浏览器逐次上传下载验证一级黑体、二级宋体、三级黑体及原流程。

以下保留 2026-09-17 初版执行记录；涉及“三项控件/全范围”的描述已由本次扩展更新。

2026-09-17 执行记录：用户最新验收为 Python 在 WSL 运行，前端上传、修改固定参数、下载真实修改后的 DOCX。本轮核心流程已通过 11 项单元测试和真实 Chromium 上传下载检查，见 VERIFICATION.md。原计划第 6 节及更广的试点条件仍保留，未完成项目不计入本轮核心验收。2026-09-18 已将当前目录关联到 `https://github.com/GZUCMHjy/wenxu-docx.git` 的 `master`，保留原有提交历史。

## 0. 开工准备

- [x] WSL Ubuntu-24.04 / Python 3.12.3；独立虚拟环境位于 Linux 文件系统。
- [x] 安装并记录 Streamlit 1.55.0、python-docx 1.2.0、lxml 6.0.2；LibreOffice 移至后续阶段。
- [x] 构造中文普通段落、混合 run、表格、图片、超链接及页眉页脚样例，另准备损坏与伪装输入。真实业务样本另行收集。
- [x] 固定受支持输入声明：普通 `.docx`；不支持 `.doc`、加密/损坏文件及无法验证的复杂结构。

验收：新环境可按 README 重建；样本不含试点用户敏感信息。

## 1. 建立最小项目

- [x] 新建 `requirements.txt`，固定三个运行依赖；浏览器测试依赖单列 `requirements-dev.txt`。
- [x] 新建 `docx_formatting.py`、`app.py`、`tests/test_docx_formatting.py`、`README.md`。
- [x] 写 README 的 WSL 启动命令、支持边界、会话内存处理和当前限制。

验收：`streamlit run app.py` 能启动空白 POC 页面；`python -m unittest discover -s tests` 可运行。

## 2. 输入预检与保全基线

- [x] 先写失败测试：拒绝非 DOCX ZIP、损坏 ZIP、空文件和超过 10 MB 上限的文件。
- [x] 基于 ZIP 包结构、内容类型及主文档 XML 校验实际 DOCX。
- [x] 检查不支持特征，错误信息可展示；拒绝 DTD、宏、签名、外部模板及正文复杂结构。
- [x] 比较主文档全部非修改 XML 和其他部件字节；范围覆盖正文、表格、图片与关系。
- [x] 改写后重新打开并验证每个目标属性；故障注入能发现正文、段后距和图片变化。

验收：有效支持样本可往返；损坏/伪装/超限输入明确失败；原始字节不被覆盖。

## 3. 实现属性级修改

- [x] 字体大小、字体颜色、行距分别与组合测试，验证只修改目标属性。
- [x] 实现 `apply_formatting(docx_bytes, options) -> (output_bytes, change_summary)`；仅接收已启用字段。
- [x] 更新正文、表格及超链接已有 runs；复杂文字字号与主题颜色覆盖通过专项测试。
- [x] 行距仅更新对应属性，保留字体、字号、缩进和段前段后。
- [x] 空段落不造文本，未选属性不写回；不支持结构返回错误。
- [x] 内存中构造输出，不覆盖原稿；摘要与 `_vN` 文件名可用；重复相同直接格式返回原字节。

验收：字号、颜色、行距单项和组合用例通过；正文字符与顺序一致；粗体/斜体等其他 run 格式保持；原稿文件哈希不变。

## 4. 结果校验

- [x] 输出后再次读取 DOCX，比较正文、未选属性和目标值。
- [x] 目标属性逐项检查，未选属性使用屏蔽允许变化后的完整 XML 对比。
- [x] 主文档以外所有部件逐字节保全，检查失败阻断下载。
- [x] 输入不支持、格式值无效、写回失败和校验失败均有用户可读提示。

验收：验证失败时不会返回“成功”状态或 DOCX 下载按钮。

## 5. Streamlit 交互

- [x] 单文件上传、10 MB 限制、示例下载可用。
- [x] 颜色选择器、字号数值、固定/倍数行距三个独立启用项，默认不启用。
- [x] 明示包括标题、表格和超链接的主体范围；页眉页脚保留。
- [x] 无选项不能执行；点击执行后预检失败则中止、不生成结果。
- [x] 成功展示摘要与独立下载；参数或输入变化立即撤下旧结果。
- [x] 不显示未经验证的规范合格结论。

本轮证据：真实浏览器成功、无选项、单项修改、损坏文件和旧结果失效通过；超限文件在引擎单元测试中通过，尚未独立执行超限文件的浏览器路径。

## 6. 渲染预览与兼容性检查（后续阶段）

- [ ] 对校验通过的输出调用 LibreOffice headless，将 DOCX 转成 PDF；超时、渲染错误或 PDF 空文件均显示失败，不复用旧预览。
- [ ] 给原稿和输出生成对照预览；记录渲染器版本与缺字体信息。
- [ ] 对脱敏代表样本用 WPS 或 Microsoft Word 打开输出，核对字体/字号/行距、中文显示、表格、图片、页眉页脚及内容。
- [ ] 将已验证样本和环境条件记入 README 的“验证记录”；未验证的结构明确标记不支持。

验收：DOCX 可打开、内容校验通过且预览成功才显示完整成功结果；缺字体或结果内容不一致阻断交付。

## 7. 交付与后续试点

- [ ] 从干净环境按 README 重建并完整执行测试。
- [ ] 用每一份支持样本重复处理至少 3 次，输出校验结论一致。
- [x] 输入字节未改动；应用文件处理全在会话内存，无上传临时目录；内存由会话/进程生命周期释放。
- [x] README 记录已支持/不支持列表与已知限制；账号、历史、批处理、模板库和 Agent 未实现。

本轮核心验收已满足；真实试点/完整旧计划验收仍需完成第 6 节和本节尚未勾选的扩展检查，不将核心流程通过混同于完整兼容性通过。
